Context: This is the Treatment tab inside the patient's "Clinical Chart & Health Record" (EHR Viewer modal) — the historical record of all procedures performed on a patient across all visits, separate from the active "Current Session & Billing Workspace."
Treatment history currently shows each service as a flat, separate card (e.g. "Teeth Cleaning" and "Tooth Filling" shown as two unrelated entries), even when multiple services were performed in the same session/visit (e.g. both added to the same invoice during one "Complete Session & Queue Invoice" submission).
Fix: group treatment records by the session/visit they belong to (likely tied by appointment_id or a session_id already present on each treatment record, per the earlier fix that made each service write its own row).

Add a grouping header per visit (e.g. "Jun 27, 2026 — Session at Clinic Branch" or similar, showing date/branch/dentist once at the top).
Nest the individual services performed in that visit underneath (Teeth Cleaning, Tooth Filling, etc.), each still showing its own tooth number (if applicable), clinical notes, and record ID.
Confirm the underlying treatment records do share a common identifier linking them to the same visit — check Supabase schema for the treatment records table to confirm what field ties them together (e.g. appointment_id). If no such link currently exists, add one and backfill/migrate existing records where possible, or at minimum ensure all new records correctly share it going forward.
Keep the existing expand/collapse behavior per individual service, just nested within the visit group instead of flat at the top level.

Add/update tests in __tests__ covering the grouping logic (e.g. multiple treatment records with the same session/appointment ID render under one group; records from different sessions render as separate groups).
Verify with npx tsc --noEmit, npm run lint, and run the test suite. Visually confirm in the patient's Clinical Chart & Health Record → Treatment tab that services from the same visit are grouped together, and services from different visits remain separate.