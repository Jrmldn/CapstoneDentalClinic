// The `server-only` package is provided by Next.js at build time to fence
// server modules off from client bundles. It has no runtime API, so under
// Vitest (plain Node) we alias it to this empty module.
export {}
